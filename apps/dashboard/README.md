# Dashboard (placeholder)

Per `PHASES.md`, the Dashboard is **Phase 9** — not started. This
directory exists now only to establish the monorepo folder structure
(`apps/api`, `apps/dashboard`) required by Phase 2's Architecture &
Project Setup.

No React code, build config, or dependencies belong here until Phase 9
is reached and approved. See `docs/ARCHITECTURE_OVERVIEW.md` for how
the Dashboard will fit into the Presentation layer alongside the
Express API.
