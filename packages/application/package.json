{
  "name": "@trading-platform/application",
  "version": "0.1.0",
  "private": true,
  "description": "Application layer: ports, error hierarchy, event dispatcher. Depends only on Domain.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "dependencies": {
    "@trading-platform/domain": "workspace:*"
  }
}
